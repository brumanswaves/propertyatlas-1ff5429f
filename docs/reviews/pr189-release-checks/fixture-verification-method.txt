import fs from 'node:fs';
import path from 'node:path';
import {createHash} from 'node:crypto';
import {execFileSync,spawnSync} from 'node:child_process';
import ts from '../../node_modules/typescript/lib/typescript.js';
const dir='artifacts/pr189-fixture-correction';
const file='scripts/fixtures/print-readiness/index.tsx';
const before=execFileSync('git',['show',`1b4c74da51c745e92827a0b54831a1ae09c2004e:${file}`],{encoding:'utf8'});
const after=fs.readFileSync(file,'utf8').replaceAll('\r\n','\n');
const hash=x=>createHash('sha256').update(x).digest('hex');
if(hash(after)!=='d5483dd2473e45663939fa40e62ee9870c75c2c2d3e1fcbbfbcab88b9928136d')throw Error('Corrected fixture differs from approved exact LF bytes');
const options={target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.ESNext,jsx:ts.JsxEmit.ReactJSX,sourceMap:false,removeComments:false};
const a=ts.transpileModule(before,{compilerOptions:options,fileName:'index.tsx',reportDiagnostics:true});
const b=ts.transpileModule(after,{compilerOptions:options,fileName:'index.tsx',reportDiagnostics:true});
if(a.outputText!==b.outputText||a.diagnostics.length||b.diagnostics.length)throw Error('Emitted runtime or transpilation diagnostics changed');
function anys(s){let count=0;const visit=n=>{if(n.kind===ts.SyntaxKind.AnyKeyword)count++;ts.forEachChild(n,visit);};visit(ts.createSourceFile('index.tsx',s,ts.ScriptTarget.Latest,true,ts.ScriptKind.TSX));return count;}
const declaration=after.slice(after.indexOf('const w ='),after.indexOf('\nw.fixture='));
const probe=`${declaration}\nw.fixture={account:'a',order:'o',version:'v',previewCalls:[],versionData:undefined};\nw.fixture.account=null;\nw.fixture.account='b';\nw.fixture.order='o2';\nw.fixture.version='v2';\nw.fixture.previewCalls.push({orderId:'o',assetId:'a',versionId:'v',preview:true});\nw.fixture.previewCalls.push({orderId:'o',assetId:'a'});\nw.fixture.versionData={opaque:true};\nw.change=(kind:string)=>{if(kind==='signout')w.fixture.account=null;};\nw.change('signout');\nexport {};\n`;
const probePath=dir+'/window-contract.ts';fs.writeFileSync(probePath,probe);
const program=ts.createProgram([probePath],{strict:true,noEmit:true,skipLibCheck:true,target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.ESNext,lib:['lib.es2022.d.ts','lib.dom.d.ts'],types:[]});
const diagnostics=ts.getPreEmitDiagnostics(program);
if(diagnostics.length)throw Error(diagnostics.map(d=>ts.flattenDiagnosticMessageText(d.messageText,'\n')).join('\n'));
const oldCommand=JSON.parse(fs.readFileSync('docs/reviews/pr189-release-checks/changed-lint-command.json','utf8')).command;
const files=oldCommand.slice(oldCommand.indexOf('scripts/fixtures/')).split(' ');
if(files.length!==10)throw Error('Expected original ten-file command');
const args=['node_modules/eslint/bin/eslint.js','--no-warn-ignored','--rule','prettier/prettier: off',...files];
const startedAt=new Date().toISOString();
const env=Object.fromEntries(['PATH','SystemRoot','WINDIR','TEMP','TMP','COMSPEC','PATHEXT'].filter(k=>process.env[k]).map(k=>[k,process.env[k]]));
env.NODE_OPTIONS='--import=./scripts/verify-shared-investigation-network.mjs';env.NO_COLOR='1';
const result=spawnSync(process.execPath,args,{encoding:'utf8',env,maxBuffer:4*1024*1024});
const output=((result.stdout||'')+(result.stderr||'')).replaceAll(process.cwd(),'[candidate-worktree]').replace(/\x1b\[[0-9;]*m/g,'').replace(/[ \t]+$/gm,'').trimEnd()+'\n';
fs.writeFileSync(dir+'/lint.txt',output);
const receipt={baseline:'1b4c74da51c745e92827a0b54831a1ae09c2004e',target:file,originalGitBlob:execFileSync('git',['rev-parse',`HEAD:${file}`],{encoding:'utf8'}).trim(),correctedLfSha256:hash(after),typeScriptVersion:ts.version,node:process.version,compilerOptions:options,emittedJavaScript:{byteIdentical:true,beforeSha256:hash(a.outputText),afterSha256:hash(b.outputText),diagnosticsBefore:a.diagnostics.length,diagnosticsAfter:b.diagnostics.length},explicitAnyNodes:{before:anys(before),after:anys(after)},strictWindowContract:{diagnostics:diagnostics.length,exitCode:0,scope:'Introduced declaration plus null signout, selection reassignment, opaque versionData and optional preview fields; not whole-app typecheck'},lint:{command:oldCommand,fileCount:files.length,startedAt,endedAt:new Date().toISOString(),exitCode:result.status,signal:result.signal},tsconfigScope:'Repository tsconfig includes src and root configuration, not scripts fixtures; prior full TypeScript evidence remains unchanged.',additionalDiscretionarySpend:0};
fs.writeFileSync(dir+'/verification.json',JSON.stringify(receipt,null,2)+'\n');
console.log(JSON.stringify(receipt,null,2));
if(result.status!==0)process.exit(result.status||1);
