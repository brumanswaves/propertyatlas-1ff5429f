# Easy Erf: verified checkpoint, 19 September 2026

## Outcome and boundaries

The PR #185 SG implementation is demonstrably served on the customer domain. The separately approved Ask-at-top report candidate has been integrated with merged main and repaired on the existing isolated single-writer branch. This does not establish whole-site live acceptance or a live commercial launch.

Owner limits for this pass: do not publish, merge a PR, roll back, repeat PR #185 review, or rerun full CI. One still-unused synthetic zero-AI upload only after preservation preflight. No real documents, Read diagram, Retry reading, processing consent, paid provider, real payment, contact, credentials/settings changes or report publication. Additional discretionary cap $0. Existing model and reasoning retained; no agents dispatched.

## Source and preservation

- Canonical repository: https://github.com/brumanswaves/propertyatlas-1ff5429f
- Merged main: `1fef6f66e1d911aaf492567f9e8c2f04eae13674`.
- PR #185 receipt references: `5744637018`, `5744686189`. Existing publication: `13f13e80-1983-4785-becb-e571f7aff497`. Its publish action is consumed.
- Report branch: `codex/report-decision-brief-181`. The two approved report commits were rebased onto merged main. Original approved head remains on local safety branch `codex/report-decision-brief-approved-9762338c`; remote report branch remains unchanged. The implementation commit following this record identifies the resulting local candidate.
- Original dirty workspace and separate SG checkout preserved. No remote force-push or PR created. Creating a PR would automatically run the broad workflow, which is excluded in this pass.
- The shared dependency junction was preserved under ignored `artifacts/node_modules`; this checkout now has its own dependencies. `npm ci` found pre-existing manifest/lock disagreement, including Lovable config 2.12.0 versus lock 2.5.3. Used the repository CI installation approach with `--package-lock=false`; manifests and lockfile unchanged. This is not a reproducible-lock acceptance claim.

## Publication diagnosis

Fresh cache-bypassed HTTP responses from both https://easyerf.co.za and https://easyerf.lovable.app served the same relevant assets. No browser storage or drafts cleared.

| Asset | SHA256 of fetched content | Evidence |
| --- | --- | --- |
| `/assets/index-CUax4t-0.js` | `F4BDB296810D1EC828160F7F5EC889E69BEA663D147717819BC4D3306FE54858` | SG receipt SHA256, Refresh file status and linked preview worker present |
| `/assets/index-Ds-R1IZH.js` | `C107CF358616A697EE052E00447274E21D556E51ED30822C019BFB7A46C329BB` | Additional linked application module |
| `/assets/sgPreview.worker-BJCrIS66.js` | `27F3054CB58CC5E52625DB94327CBC609B1EFD7A0F4514762E40158389F0A6F7` | HTTP 200, 110729 bytes, Group 3 option handling and TIFF decoder present |

Prior served modules `index-BI1QdPDk.js` and `index-fkR-_VoC.js` have been replaced. Browser SG UI independently showed original receipt/preview separated from optional interpretation. Lovable publication panel showed Published / website up to date / disabled Publish changes. Exact publication-ID-to-Git-SHA attestation remains UNKNOWN; generic published metadata was not used as sufficient evidence.

## Approved synthetic acceptance: blocked before upload, unconsumed

Exact TEST order `fd14b0c1-410d-4aec-beaa-5b5d133e56cf`, canonical Erf 1570 parcel and owner were rechecked. Two original attachments were present; neither was automatically processing. Processing permissions were off. No automatic extraction poll was observed or eligible from their stored status.

Local synthetic file only: `SYNTHETIC-QA-PR185-NOT-PROPERTY-EVIDENCE.tiff`, 7681000 bytes, SHA256 `95a80568c63a2779218ab268affbce2201898bc4345a1c118cb553dbf9171a5b`.

The file chooser rejected `setFiles` with Not allowed before any storage/upload/extraction request. the one-shot has NOT been consumed. Chrome requires the owner to enable **Allow access to file URLs** on the ChatGPT browser extension. No bypass attempted. Same-bytes reselection, real stored-byte comparison, desktop/mobile persisted preview, continue/reopen and removal remain unperformed.

Backend row fingerprints confirmed the TEST order and both original attachments unchanged at the end of the pass. Browser non-auth draft content was compared privately: 65 keys preserved; only the workspace updatedAt field changed during navigation. No draft conflict was resolved or overwritten.

## Report corrections completed locally

- Canonical Market (`listings`) and Documents (`reports`) evidence links open the correct report section.
- Finding actions preserve the exact SG anchor, matching the canonical summary action.
- Property checks, municipal services and location actions work when an editable caller exists; read-only or frozen versions show an honest unavailable explanation instead of enabled no-op buttons.
- The optional development gap now uses accepted deterministic envelope state or explicit skip. A legacy generated concept does not count as acceptance. The existing gap ID stays stable; unverified planning gaps remain. Self-service report construction carries the same accepted-envelope signal already supplied by shared investigation assembly.
- SG guidance explains receipt/preview, continuation without AI and explicitly chosen interpretation. It no longer suggests mandatory automatic reading.
- Ask remains at the top. Calculations, source evidence, reviewed snapshots, document permissions and payment controls remain unchanged.

## Verification

- Final focused evidence/report/investigation tests: 36 files, 566 tests passed with this checkout's own dependencies.
- Sign-in/payment/handoff/queue/guided/shared-investigation checks: 23 files, 220 tests passed. These are source tests, not a live payment or customer acceptance claim.
- TypeScript, production node-server build and diff checks passed. Targeted ESLint passed using the existing workflow's `prettier/prettier: off` rule. Plain lint remains affected by existing formatting violations; no blanket formatting was applied.
- Browser: real report component with synthetic sparse and supported/conflicting fixtures at 390x844 and 1440x1000. Ask is above assessment and visible in first viewport, three summary actions, six evidence sections, no horizontal overflow, material contradiction visible. Market evidence reveal, return focus and exact order hash, SG callback and unsent Ask text retention verified. No question submitted.
- Stable final action interval: network observer reported no HTTP request, no truncation and no further events. Across navigation, the debugger buffer was truncated, so that broader interval is NOT continuous no-traffic proof. No browser error in the final inspected log.
- Initial blank local reload was isolated to a preserved dependency junction being scanned under artifacts; naming it artifacts/node_modules restored HTTP and browser service. No product code was changed for that tooling issue.
- No full local suite or remote CI rerun. Existing report design's historical broader acceptance is documented separately and must not be represented as new exact-head evidence. Full real-route, server-persisted combined journey remains a release gate.

## Live commercial gate

Read-only `/admin/launch-readiness` at 2026-09-19 21:59:43 Africa/Johannesburg, request `74cefe98-dcce-4540-b30c-fa9cb058c791`:

- VERIFIED UI: runtime TEST, live arming OFF.
- VERIFIED UI: configured TEST key, one-time R999 ZAR test Payment Link, reports return URL and expected test webhook event configuration passed.
- VERIFIED UI: account charging/payout capability passed, but Easy Erf business profile FAILED because business name or website does not correctly identify Easy Erf. This must not be treated as the correct commercial account merely because capabilities pass.
- UNKNOWN: webhook signing-secret match. Presence is not proof of match.
- No payment/settings mutation, event replay, order creation or charge performed. Do not attempt live checkout while these gates remain.

## Remaining work and exact resume

1. Owner enables the browser extension's file URL setting. Recheck one-shot consumption, exact owner/TEST order, draft/file baselines and no automatic polling. Then perform only the approved one synthetic upload, same bytes, desktop/mobile preview, continue/reopen, original-byte proof, no duplicates/extraction and application removal of only the new attachment. No production save/processing beyond that exact approval.
2. Report source is locally reviewable; no merge or publication. A later authorized review/CI/release step must use the exact candidate and preserve the single-writer workflow.
3. Whole-site combined live customer acceptance remains unfinished. The user's desired map -> sign-in -> exact erf -> guided investigation -> report -> optional human takeover is the acceptance contract, not a completion claim.
4. Correct Stripe business identity/configuration and prove signed event handling only under specific owner-controlled authority. TEST mode is not a live purchase.
5. A Lovable accessibility snapshot exposed a token-bearing preview URL during inspection. It was not used, decoded or copied into this record. Credential revocation remains an owner action; no credential was changed.

No builder is implied by this document. Preview servers are validation processes only. $0 additional discretionary cap; no paid processing or credit purchase initiated; actual account-wide spend UNKNOWN.
