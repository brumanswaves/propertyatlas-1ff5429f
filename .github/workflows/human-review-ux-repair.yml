name: Human Review UX repair

on:
  push:
    branches:
      - chatgpt/fix-human-review-ux
    paths:
      - .github/workflows/human-review-ux-repair.yml

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - name: Apply coherent Human Review repair
        env:
          GITHUB_TOKEN: ${{ github.token }}
          HEAD_REF: chatgpt/fix-human-review-ux
        run: |
          set -euo pipefail
          git init .
          git remote add origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git fetch --no-tags --depth=50 origin "${HEAD_REF}"
          git checkout -B "${HEAD_REF}" FETCH_HEAD

          npx --yes supabase@latest migration new reopen_easy_erf_human_review
          MIGRATION_PATH="$(find supabase/migrations -maxdepth 1 -type f -name '*_reopen_easy_erf_human_review.sql' | sort | tail -1)"
          test -n "${MIGRATION_PATH}"
          export MIGRATION_PATH

          python - <<'PY'
          from pathlib import Path
          import os

          def replace_once(path: str, old: str, new: str):
              p = Path(path)
              text = p.read_text()
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{path}: expected one match, found {count}")
              p.write_text(text.replace(old, new, 1))

          # 1) Home = map + two obvious choices.
          old_home = '''      {!selected && !selectedOfficial && !showOfficialReopenCard && !searchHighlight && (
        <div className="pointer-events-auto absolute bottom-20 left-1/2 z-20 w-[min(94vw,42rem)] -translate-x-1/2 rounded-2xl border border-accent/30 bg-card/95 p-3 shadow-panel backdrop-blur md:bottom-8">
          <div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
            <div>
              <div className="text-[10px] font-semibold uppercase tracking-wider text-accent">
                R999 · Human-reviewed
              </div>
              <div className="mt-1 text-sm font-semibold text-foreground">
                Want Easy Erf to investigate the property with human review?
              </div>
              <div className="mt-1 text-[11px] leading-relaxed text-muted-foreground">
                One property. Property Truth, potential, risks, unknowns and clear next steps.
              </div>
            </div>
            <Link
              to="/pricing"
              className="inline-flex shrink-0 items-center justify-center rounded-full bg-accent px-4 py-2 text-xs font-semibold text-accent-foreground shadow-soft hover:bg-accent/90"
            >
              See the R999 review
            </Link>
          </div>
        </div>
      )}'''
          new_home = '''      {!selected && !selectedOfficial && !showOfficialReopenCard && !searchHighlight && (
        <div className="pointer-events-auto absolute bottom-20 left-1/2 z-20 w-[min(94vw,40rem)] -translate-x-1/2 rounded-3xl border border-white/20 bg-card/95 p-4 shadow-panel backdrop-blur md:bottom-8">
          <div className="text-center">
            <div className="text-[10px] font-semibold uppercase tracking-wider text-muted-foreground">
              Choose how you want to investigate
            </div>
            <div className="mt-1 text-base font-semibold text-foreground">
              Do it yourself, or hand it to Easy Erf for human review.
            </div>
          </div>
          <div className="mt-4 grid gap-2 sm:grid-cols-2">
            <button
              type="button"
              onClick={() => window.dispatchEvent(new Event("easy-erf:start-self-review"))}
              className="inline-flex min-h-11 items-center justify-center rounded-full border border-border bg-background px-5 py-2.5 text-sm font-semibold text-foreground shadow-soft hover:bg-muted"
            >
              Investigate it myself
            </button>
            <Link
              to="/pricing"
              className="inline-flex min-h-11 items-center justify-center rounded-full bg-accent px-5 py-2.5 text-sm font-semibold text-accent-foreground shadow-soft hover:bg-accent/90"
            >
              Get Human Review · R999
            </Link>
          </div>
        </div>
      )}'''
          replace_once("src/routes/index.tsx", old_home, new_home)

          # 2) Home self-review CTA opens the real existing search lane chooser.
          search_anchor = '''  const [pilotRegistry, setPilotRegistry] = useState<PilotParcelRegistry | null>(null);
  const [pilotRegistryMessage, setPilotRegistryMessage] = useState<string | null>(null);

  useEffect(() => {
    let cancelled = false;'''
          search_replacement = '''  const [pilotRegistry, setPilotRegistry] = useState<PilotParcelRegistry | null>(null);
  const [pilotRegistryMessage, setPilotRegistryMessage] = useState<string | null>(null);

  useEffect(() => {
    const openSelfReview = () => {
      setLane(null);
      setOpen(true);
    };
    window.addEventListener("easy-erf:start-self-review", openSelfReview);
    return () => window.removeEventListener("easy-erf:start-self-review", openSelfReview);
  }, []);

  useEffect(() => {
    let cancelled = false;'''
          replace_once("src/components/map/SearchBar.tsx", search_anchor, search_replacement)

          # 3) Pricing becomes Human Review and resolves checkout server-side in TEST mode.
          replace_once(
              "src/routes/pricing.tsx",
              'import { createFileRoute, Link } from "@tanstack/react-router";\n',
              'import { createFileRoute, Link } from "@tanstack/react-router";\nimport { useState } from "react";\n',
          )
          replace_once(
              "src/routes/pricing.tsx",
              'import { Footer } from "@/components/layout/Footer";\n',
              'import { Footer } from "@/components/layout/Footer";\nimport { supabase } from "@/integrations/supabase/client";\n',
          )
          old_link_fn = '''function configuredEarlyAccessPaymentLink() {
  const value = import.meta.env.VITE_EASY_ERF_R999_PAYMENT_LINK?.trim();
  if (!value) return null;

  try {
    const url = new URL(value);
    if (url.protocol !== "https:" || url.hostname !== "buy.stripe.com") return null;
    return url.toString();
  } catch {
    return null;
  }
}

function PricingPage() {
  const earlyAccessPaymentLink = configuredEarlyAccessPaymentLink();
'''
          new_link_fn = '''function PricingPage() {
  const [checkoutLoading, setCheckoutLoading] = useState(false);
  const [checkoutError, setCheckoutError] = useState<string | null>(null);

  async function startHumanReviewCheckout() {
    setCheckoutLoading(true);
    setCheckoutError(null);
    try {
      const { data, error } = await supabase.functions.invoke("easy-erf-r999-checkout");
      if (error || !data?.ok || data?.mode !== "test" || typeof data?.url !== "string") {
        throw new Error(data?.error ?? error?.message ?? "Secure checkout is unavailable.");
      }
      const url = new URL(data.url);
      if (url.protocol !== "https:" || url.hostname !== "buy.stripe.com") {
        throw new Error("Secure checkout returned an invalid destination.");
      }
      window.location.assign(url.toString());
    } catch (error) {
      setCheckoutError(error instanceof Error ? error.message : "Secure checkout is unavailable.");
    } finally {
      setCheckoutLoading(false);
    }
  }
'''
          replace_once("src/routes/pricing.tsx", old_link_fn, new_link_fn)
          replace_once(
              "src/routes/pricing.tsx",
              '<Sparkles className="h-3 w-3 text-accent" /> Pricing',
              '<Sparkles className="h-3 w-3 text-accent" /> Human Review',
          )
          replace_once(
              "src/routes/pricing.tsx",
              '            Start investigating without a subscription.',
              '            Get a human-reviewed property investigation.',
          )
          replace_once(
              "src/routes/pricing.tsx",
              '            Use the core investigation yourself, or choose Early Access when you want Easy Erf to do more of the investigation with human review.',
              '            If you want Easy Erf to take over the investigation, choose Human Review. You can still investigate the property yourself at any time.',
          )
          replace_once(
              "src/routes/pricing.tsx",
              '                Early Access',
              '                Human Review',
          )
          old_checkout = '''              {earlyAccessPaymentLink ? (
                <a
                  href={earlyAccessPaymentLink}
                  target="_blank"
                  rel="noreferrer"
                  className="mt-6 inline-flex items-center gap-2 rounded-full bg-accent px-5 py-2.5 text-sm font-semibold text-accent-foreground shadow-soft hover:bg-accent/90"
                >
                  Start R999 Investigation
                  <ArrowUpRight className="h-4 w-4" />
                </a>
              ) : (
                <div className="mt-6">
                  <button
                    type="button"
                    disabled
                    className="inline-flex cursor-not-allowed items-center gap-2 rounded-full bg-muted px-5 py-2.5 text-sm font-semibold text-muted-foreground"
                  >
                    Secure checkout is being connected
                  </button>
                  <p className="mt-2 max-w-md text-xs leading-relaxed text-muted-foreground">
                    Easy Erf will only enable this button when the verified Stripe-hosted checkout is configured. No payment is collected by this page while checkout is unavailable.
                  </p>
                </div>
              )}'''
          new_checkout = '''              <div className="mt-6">
                <button
                  type="button"
                  disabled={checkoutLoading}
                  onClick={() => void startHumanReviewCheckout()}
                  className="inline-flex items-center gap-2 rounded-full bg-accent px-5 py-2.5 text-sm font-semibold text-accent-foreground shadow-soft hover:bg-accent/90 disabled:opacity-60"
                >
                  {checkoutLoading ? "Opening secure checkout…" : "Start Human Review — R999"}
                  <ArrowUpRight className="h-4 w-4" />
                </button>
                <p className="mt-2 max-w-md text-xs leading-relaxed text-muted-foreground">
                  Secure Stripe-hosted checkout. The current launch configuration is TEST mode only.
                </p>
                {checkoutError ? (
                  <p className="mt-2 max-w-md text-xs font-medium text-destructive">{checkoutError}</p>
                ) : null}
              </div>'''
          replace_once("src/routes/pricing.tsx", old_checkout, new_checkout)

          # 4) Founder Operations order title becomes a direct action into the editable queue.
          old_admin_order = '''                          <div className="font-semibold text-foreground">{order.report_type}</div>
                          <div className="mt-1 font-mono text-[10px] text-muted-foreground">
                            {shortId(order.id)}
                          </div>'''
          new_admin_order = '''                          <a
                            href={`/admin/fulfillment#order-${order.id}`}
                            className="font-semibold text-accent hover:underline"
                          >
                            {order.report_type}
                          </a>
                          <div className="mt-1 font-mono text-[10px] text-muted-foreground">
                            {shortId(order.id)} · Open / change review
                          </div>'''
          replace_once("src/routes/admin.tsx", old_admin_order, new_admin_order)
          replace_once(
              "src/routes/admin.tsx",
              'description="Read-only payment and fulfilment truth from the secured order ledger."',
              'description="Open an order to review, replace or redeliver the human-reviewed report while keeping payment truth read-only."',
          )

          # 5) Founder fulfillment can reopen a completed report for replacement/re-review.
          replace_once(
              "src/routes/admin.fulfillment.tsx",
              '  ReceiptText,\n  Upload,',
              '  ReceiptText,\n  RotateCcw,\n  Upload,',
          )
          replace_once(
              "src/routes/admin.fulfillment.tsx",
              'type FulfillmentAction = "start_review" | "mark_ready" | "mark_failed";',
              'type FulfillmentAction = "start_review" | "reopen_review" | "mark_ready" | "mark_failed";',
          )
          old_toast = '''      action === "start_review"
        ? "Human review started"
        : action === "mark_ready"
          ? "Report marked ready"
          : "Order marked failed",'''
          new_toast = '''      action === "start_review"
        ? "Human review started"
        : action === "reopen_review"
          ? "Report reopened for review"
          : action === "mark_ready"
            ? "Report marked ready"
            : "Order marked failed",'''
          replace_once("src/routes/admin.fulfillment.tsx", old_toast, new_toast)
          queue_effect_anchor = '''  useEffect(() => {
    void refresh();
  }, [refresh]);

  async function transition('''
          queue_effect_replacement = '''  useEffect(() => {
    void refresh();
  }, [refresh]);

  useEffect(() => {
    if (loading || typeof window === "undefined" || !window.location.hash.startsWith("#order-")) return;
    const id = window.location.hash.slice(1);
    window.requestAnimationFrame(() => {
      document.getElementById(id)?.scrollIntoView({ behavior: "smooth", block: "center" });
    });
  }, [loading, orders]);

  async function transition('''
          replace_once("src/routes/admin.fulfillment.tsx", queue_effect_anchor, queue_effect_replacement)
          replace_once(
              "src/routes/admin.fulfillment.tsx",
              '<article className="rounded-3xl border border-border bg-card p-5 shadow-soft">',
              '<article id={`order-${order.id}`} className="scroll-mt-28 rounded-3xl border border-border bg-card p-5 shadow-soft">',
          )
          action_anchor = '''        {status === "processing" ? (
          <ReadyAction order={order} busy={busy} onUploadReport={onUploadReport} />
        ) : null}
        {status === "paid" || status === "processing" ? ('''
          action_replacement = '''        {status === "processing" ? (
          <ReadyAction order={order} busy={busy} onUploadReport={onUploadReport} />
        ) : null}
        {status === "ready" ? (
          <button
            type="button"
            disabled={busy}
            onClick={() => void onTransition(order, "reopen_review")}
            className="inline-flex items-center gap-1.5 rounded-full border border-accent/35 bg-accent/5 px-4 py-2 text-xs font-semibold text-foreground disabled:opacity-50"
          >
            <RotateCcw className="h-3.5 w-3.5 text-accent" /> Reopen / replace report
          </button>
        ) : null}
        {status === "paid" || status === "processing" ? ('''
          replace_once("src/routes/admin.fulfillment.tsx", action_anchor, action_replacement)

          # 6) Edge authority permits the audited reopen action.
          replace_once(
              "supabase/functions/easy-erf-founder-fulfillment/index.ts",
              'new Set(["start_review", "mark_ready", "mark_failed"])',
              'new Set(["start_review", "reopen_review", "mark_ready", "mark_failed"])',
          )

          # 7) Public TEST checkout resolver. It returns only a verified public Stripe URL.
          checkout_fn = r'''import Stripe from "npm:stripe@22.6.0";

import { parseAcceptedPaymentLinkIds } from "../_shared/easyErfStripePaymentContract.ts";

declare const Deno: {
  env: { get(key: string): string | undefined };
  serve(handler: (request: Request) => Promise<Response>): unknown;
};

const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
};

function json(payload: unknown, status = 200) {
  return new Response(JSON.stringify(payload), {
    status,
    headers: { ...corsHeaders, "Content-Type": "application/json" },
  });
}

Deno.serve(async (request: Request) => {
  if (request.method === "OPTIONS") return new Response("ok", { headers: corsHeaders });
  if (request.method !== "POST") return json({ ok: false, error: "Method not allowed." }, 405);

  const stripeSecretKey = Deno.env.get("STRIPE_SECRET_KEY")?.trim();
  const acceptedIds = parseAcceptedPaymentLinkIds(
    Deno.env.get("EASY_ERF_R999_PAYMENT_LINK_IDS"),
  );
  if (!stripeSecretKey || acceptedIds.size === 0) {
    return json({ ok: false, error: "Human Review checkout is not configured." }, 503);
  }

  const stripe = new Stripe(stripeSecretKey);
  for (const paymentLinkId of acceptedIds) {
    try {
      const link = await stripe.paymentLinks.retrieve(paymentLinkId);
      if (!link.active || link.livemode || typeof link.url !== "string") continue;
      const lineItems = await stripe.paymentLinks.listLineItems(paymentLinkId, { limit: 10 });
      if (lineItems.data.length !== 1) continue;
      const price = lineItems.data[0].price;
      if (
        !price ||
        typeof price === "string" ||
        price.unit_amount !== 99900 ||
        price.currency.toLowerCase() !== "zar" ||
        price.type !== "one_time"
      ) continue;
      const url = new URL(link.url);
      if (url.protocol !== "https:" || url.hostname !== "buy.stripe.com") continue;
      return json({ ok: true, mode: "test", url: url.toString() });
    } catch {
      continue;
    }
  }

  return json({ ok: false, error: "Verified TEST Human Review checkout is unavailable." }, 503);
});
'''
          Path("supabase/functions/easy-erf-r999-checkout").mkdir(parents=True, exist_ok=True)
          Path("supabase/functions/easy-erf-r999-checkout/index.ts").write_text(checkout_fn)

          config = Path("supabase/config.toml")
          text = config.read_text()
          text += '''\n\n# Human Review pricing resolves only the existing verified TEST Stripe Payment Link.\n# The function returns a public Stripe-hosted URL and never creates a payment itself.\n[functions.easy-erf-r999-checkout]\nverify_jwt = false\n'''
          config.write_text(text)

          # 8) Generate the reopen migration from the latest canonical transition function.
          base = Path("supabase/migrations/20260831130000_align_easy_erf_fulfillment_status_enum.sql").read_text()
          reopen_anchor = '''    when 'mark_ready' then
      if v_from_status <> 'processing' then'''
          reopen_case = '''    when 'reopen_review' then
      if v_from_status <> 'ready' then
        raise exception 'Only ready orders can be reopened for review';
      end if;
      v_to_status := 'processing';
      update public.report_orders
      set status = 'processing',
          status_enum = 'fulfilling'::public.report_order_status,
          failure_reason = null,
          completed_at = null,
          updated_at = now()
      where id = p_order_id
      returning * into v_order;

    when 'mark_ready' then
      if v_from_status <> 'processing' then'''
          if base.count(reopen_anchor) != 1:
              raise SystemExit("latest transition migration anchor mismatch")
          reopened = base.replace(reopen_anchor, reopen_case, 1)
          reopened = reopened.replace(
              "Applies audited founder fulfillment using canonical report_order_status values fulfilling/complete while preserving processing/ready product labels.",
              "Applies audited founder fulfillment, including an explicit ready-to-processing reopen for report replacement, using canonical report_order_status values fulfilling/complete.",
              1,
          )
          migration_path = Path(os.environ["MIGRATION_PATH"])
          migration_path.write_text(reopened)
          migration_name = migration_path.name

          # 9) Isolated DB proof includes ready -> reopen -> ready and four audit events.
          verify = Path("scripts/verify-easy-erf-founder-fulfillment.sql")
          verify_text = verify.read_text()
          verify_text = verify_text.replace(
              "\\i supabase/migrations/20260831130000_align_easy_erf_fulfillment_status_enum.sql\n",
              "\\i supabase/migrations/20260831130000_align_easy_erf_fulfillment_status_enum.sql\n\\i supabase/migrations/" + migration_name + "\n",
              1,
          )
          regression_anchor = '''  begin
    perform public.transition_easy_erf_report_order(
      v_order_id, 'start_review', v_actor_id, null, null
    );'''
          reopen_proof = '''  select * into v_row from public.transition_easy_erf_report_order(
    v_order_id, 'reopen_review', v_actor_id, null, null
  );
  if v_row.status <> 'processing'
     or v_row.status_enum <> 'fulfilling'::public.report_order_status
     or v_row.completed_at is not null then
    raise exception 'reopen_review did not move ready order back to processing';
  end if;

  select * into v_row from public.transition_easy_erf_report_order(
    v_order_id,
    'mark_ready',
    v_actor_id,
    v_expected_path,
    null
  );
  if v_row.status <> 'ready'
     or v_row.status_enum <> 'complete'::public.report_order_status
     or v_row.completed_at is null then
    raise exception 'reopened report could not be marked ready again';
  end if;

  begin
    perform public.transition_easy_erf_report_order(
      v_order_id, 'start_review', v_actor_id, null, null
    );'''
          if verify_text.count(regression_anchor) != 1:
              raise SystemExit("verification regression anchor mismatch")
          verify_text = verify_text.replace(regression_anchor, reopen_proof, 1)
          verify_text = verify_text.replace(
              "if v_event_count <> 2 then\n    raise exception 'Expected exactly two successful fulfillment events, got %', v_event_count;",
              "if v_event_count <> 4 then\n    raise exception 'Expected exactly four successful fulfillment events, got %', v_event_count;",
              1,
          )
          verify.write_text(verify_text)

          # 10) Guardrails: state machine, checkout resolver, visibility and migration inventory.
          guard = Path("src/lib/payments/__tests__/easyErfFounderFulfillmentGuardrails.test.ts")
          g = guard.read_text()
          g = g.replace(
              '''const deliveryMigration = source(
  "supabase/migrations/20260831113000_secure_easy_erf_report_delivery.sql",
);''',
              '''const deliveryMigration = source(
  "supabase/migrations/20260831113000_secure_easy_erf_report_delivery.sql",
);
const reopenMigration = source(
  "supabase/migrations/''' + migration_name + '''",
);''',
              1,
          )
          g = g.replace(
              'expect(deliveryMigration).not.toContain("when \'mark_paid\'");',
              '''expect(reopenMigration).toContain("when 'reopen_review' then");
    expect(reopenMigration).toContain("Only ready orders can be reopened for review");
    expect(reopenMigration).toContain("completed_at = null");
    expect(deliveryMigration).not.toContain("when 'mark_paid'");''',
              1,
          )
          g = g.replace(
              'it("accepts only the three explicit fulfillment actions", () => {',
              'it("accepts only the four explicit fulfillment actions", () => {',
              1,
          )
          g = g.replace(
              'new Set([\\"start_review\\", \\"mark_ready\\", \\"mark_failed\\"])',
              'new Set([\\"start_review\\", \\"reopen_review\\", \\"mark_ready\\", \\"mark_failed\\"])',
              1,
          )
          guard.write_text(g)

          stripe_guard = Path("src/lib/payments/__tests__/easyErfStripeFulfillmentGuardrails.test.ts")
          s = stripe_guard.read_text()
          s = s.replace(
              'const webhook = source("supabase/functions/easy-erf-stripe-webhook/index.ts");',
              'const webhook = source("supabase/functions/easy-erf-stripe-webhook/index.ts");\nconst checkout = source("supabase/functions/easy-erf-r999-checkout/index.ts");',
              1,
          )
          old_payment_surface = '''  it("keeps the public checkout disabled until a verified Stripe URL is configured", () => {
    expect(pricing).toContain("VITE_EASY_ERF_R999_PAYMENT_LINK");
    expect(pricing).toContain('url.hostname !== "buy.stripe.com"');
    expect(pricing).toMatch(/Secure checkout is being connected/);
  });'''
          new_payment_surface = '''  it("resolves only the verified TEST R999 Stripe checkout server-side", () => {
    expect(pricing).toContain('supabase.functions.invoke("easy-erf-r999-checkout")');
    expect(pricing).toContain('data?.mode !== "test"');
    expect(pricing).toContain('url.hostname !== "buy.stripe.com"');
    expect(checkout).toContain("EASY_ERF_R999_PAYMENT_LINK_IDS");
    expect(checkout).toContain("STRIPE_SECRET_KEY");
    expect(checkout).toContain("link.livemode");
    expect(checkout).toContain("price.unit_amount !== 99900");
    expect(checkout).toContain('price.currency.toLowerCase() !== "zar"');
    expect(checkout).not.toMatch(/sk_(test|live)_|plink_[A-Za-z0-9]{8,}/);
    expect(config).toMatch(/\\[functions\\.easy-erf-r999-checkout\\][\\s\\S]*verify_jwt = false/);
  });'''
          if s.count(old_payment_surface) != 1:
              raise SystemExit("stripe guardrail checkout block mismatch")
          s = s.replace(old_payment_surface, new_payment_surface, 1)
          stripe_guard.write_text(s)

          visibility = Path("src/lib/__tests__/commercialEntryVisibilityGuardrails.test.ts")
          v = visibility.read_text()
          old_vis = '''  it("keeps the R999 human-reviewed offer visible from the home map", () => {
    const source = read("src/routes/index.tsx");
    expect(source).toContain("R999 · Human-reviewed");
    expect(source).toContain("See the R999 review");
    expect(source).toContain('to="/pricing"');
  });'''
          new_vis = '''  it("keeps the two-path investigation choice visible on the home map", () => {
    const source = read("src/routes/index.tsx");
    expect(source).toContain("Investigate it myself");
    expect(source).toContain("Get Human Review · R999");
    expect(source).toContain('new Event("easy-erf:start-self-review")');
    expect(source).toContain('to="/pricing"');
  });

  it("keeps Human Review available globally during self-service and deep review", () => {
    const navigation = read("src/lib/navigation.ts");
    expect(navigation).toContain('{ to: "/pricing", label: "Human Review" }');
    expect(navigation).not.toContain('label: "R999 Review"');
  });'''
          if v.count(old_vis) != 1:
              raise SystemExit("commercial visibility guardrail mismatch")
          v = v.replace(old_vis, new_vis, 1)
          v = v.replace(
              '''    expect(source).toContain('to="/admin/fulfillment"');
    expect(source).toContain("Human-review fulfillment");''',
              '''    expect(source).toContain('to="/admin/fulfillment"');
    expect(source).toContain("Human-review fulfillment");
    expect(source).toContain("Open / change review");
    const fulfillment = read("src/routes/admin.fulfillment.tsx");
    expect(fulfillment).toContain("reopen_review");
    expect(fulfillment).toContain("Reopen / replace report");''',
              1,
          )
          visibility.write_text(v)

          nav_test = Path("src/lib/__tests__/brandNavigationGuardrails.test.ts")
          n = nav_test.read_text().replace(
              '{ to: "/pricing", label: "R999 Review" },',
              '{ to: "/pricing", label: "Human Review" },',
              1,
          )
          nav_test.write_text(n)

          inv = Path("src/lib/sitePotential/__tests__/sitePotentialRepair.test.ts")
          itext = inv.read_text()
          itext = itext.replace("expect(migrationNames).toHaveLength(33);", "expect(migrationNames).toHaveLength(34);", 1)
          itext = itext.replace(
              '      "20260831130000_align_easy_erf_fulfillment_status_enum.sql",\n',
              '      "20260831130000_align_easy_erf_fulfillment_status_enum.sql",\n      "' + migration_name + '",\n',
              1,
          )
          inv.write_text(itext)
          PY

          git diff --check
          git config user.name "Easy Erf CI Repair"
          git config user.email "actions@users.noreply.github.com"
          git add \
            src/routes/index.tsx \
            src/components/map/SearchBar.tsx \
            src/routes/pricing.tsx \
            src/routes/admin.tsx \
            src/routes/admin.fulfillment.tsx \
            src/lib/navigation.ts \
            src/lib/__tests__/brandNavigationGuardrails.test.ts \
            src/lib/__tests__/commercialEntryVisibilityGuardrails.test.ts \
            src/lib/payments/__tests__/easyErfFounderFulfillmentGuardrails.test.ts \
            src/lib/payments/__tests__/easyErfStripeFulfillmentGuardrails.test.ts \
            src/lib/sitePotential/__tests__/sitePotentialRepair.test.ts \
            scripts/verify-easy-erf-founder-fulfillment.sql \
            supabase/config.toml \
            supabase/functions/easy-erf-founder-fulfillment/index.ts \
            supabase/functions/easy-erf-r999-checkout/index.ts \
            "${MIGRATION_PATH}"
          git commit -m "Fix Human Review journey and founder report editing"
          git push origin "HEAD:${HEAD_REF}"
